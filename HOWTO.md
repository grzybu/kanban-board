# Starting local envoiromnt

Prerequirements:
* PHP 7
* Create ``.env`` file in ``src/`` directory. Sample file can be found in ``.env.dist`` file.

You can test application usung PHP built-in server:

``
php -S localhost:8000 -t src/public/
``

